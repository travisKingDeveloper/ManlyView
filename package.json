{
    "name": "react-app-original",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "gh-pages": "^1.0.0",
        "react": "^15.6.1",
        "react-dom": "^15.6.1",
        "react-scripts": "1.0.10"
    },
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build",
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test --env=jsdom",
        "serve": "serve -s build",
        "eject": "react-scripts eject",
        "servit": "npm run build && npm run serve"
    },
    "homepage": "https://traviskingdeveloper.github.io/ManlyView/" //in order to let servit work this line needs to remove
}
